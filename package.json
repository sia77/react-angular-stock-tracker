{
    "scripts": {
        "install-all": "npm install --prefix angular-stock-tracker",
        "build": "npm run build:angular",
        
        "build:angular": "npm run build --prefix angular-stock-tracker",
        "start": "\"npm start --prefix angular-stock-tracker\""
    },
    "devDependencies": {
        "concurrently": "^8.0.0",
        "typescript": "~5.7.2",
        "vite": "^6.2.0"
    }
}
