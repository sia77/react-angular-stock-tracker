{
    "scripts": {
        "install-all": "npm install --prefix react-stock-tracker && npm install --prefix angular-stock-tracker",
        "build": "npm run build:react && npm run build:angular",
        "build:react": "npm run build --prefix react-stock-tracker",
        "build:angular": "npm run build --prefix angular-stock-tracker",
        "start": "concurrently \"npm start --prefix react-stock-tracker\" \"npm start --prefix angular-stock-tracker\""
    },
    "devDependencies": {
        "concurrently": "^8.0.0",
        "vite": "^6.2.0"
    }
}
